import { TestBed, inject } from '@angular/core/testing';

import { swtichService } from './swticher.service';


describe('swtichService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      imports: [            
      ],
      providers: [
        swtichService
      ]
    });
  });

});
